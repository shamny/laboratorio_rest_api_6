import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterCommentContainer } from 'pods/character-comment';

export const CharacterCommentScene = () => (
  <AppLayout>
    <CharacterCommentContainer />
  </AppLayout>
);